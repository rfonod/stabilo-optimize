# README

To keep the repository size manageable, only a single representative (downscaled) visualization video is included in this folder. Users can generate full benchmarking visualizations by running the provided benchmarking configuration on the sample dataset. 